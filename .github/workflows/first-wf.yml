name: first-wf
on: push
jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run: echo "hello world!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Say goodbye
              run: echo "goodbye all!!"          